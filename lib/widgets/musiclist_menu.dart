import 'package:flutter/material.dart';

class MusicListMenu extends StatelessWidget {
  const MusicListMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
